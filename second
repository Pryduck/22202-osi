#include <stdio.h>
#include <unistd.h>
#include <sys/types.h>

int main() {
    printf("PID процесса: %d\n", getpid());
    sleep(1);
    execl("sec", "sec", NULL);
    printf("Hello world\n");
    return 0;
}
